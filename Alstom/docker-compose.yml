services:
  ICDDB:
    image: mongo

  spectrailtestdataprovider.api:
    image: ${DOCKER_REGISTRY-}spectrailtestdataproviderapi
    build:
      context: .
      dockerfile: SpectrailTestServer/SpectrailTestDataProvider.API/Dockerfile
  portainer:
    image: portainer/portainer-ce
volumes:
  mongo_data:
  portainer_data: